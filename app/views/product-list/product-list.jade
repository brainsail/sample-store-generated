div.productList
  h3 {{vm.name}}
  ul
    li(ng-repeat="product in vm.products")
      a(ui-sref="productDetail({id: product.id})" us-sref-opts="product" ng-click="vm.selectProduct(product)"){{product.name}}
